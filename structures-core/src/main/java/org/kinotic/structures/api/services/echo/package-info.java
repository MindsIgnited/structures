/**
 * Continuum requires a version on every published interface or one of its ancestor packages, and this
 * package sits outside the tree whose version the other published services pick up.
 */
@Version("1.0.0")
package org.kinotic.structures.api.services.echo;

import org.kinotic.continuum.api.annotations.Version;
